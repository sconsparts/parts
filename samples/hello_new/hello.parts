Import('*')

PartName("hello")
PartVersion("1.0.0")

build.DependsOn([])

@build
def config(env):
    print("Called Config")

@build
def build(env):
    print("Called build-source")
    ret=env.Program("hello","hello.c")
    env.InstallTarget(ret)
