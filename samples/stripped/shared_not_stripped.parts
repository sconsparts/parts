Import('*')

env.PartName('shared_not_stripped')
env.PartVersion('1.0.0')

# This parts file will generate not stripped shared object (DLL) from stripped.cpp source code.

env.InstallTarget(
    env.SharedLibrary(
        target = '${PART_NAME}',
        source = 'stripped.cpp'
    )
)

# vim: set et ts=4 sw=4 ai ft=python :

