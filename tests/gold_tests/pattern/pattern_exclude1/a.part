Import("*")

files = Pattern(includes="*.cpp", excludes=['*.cc']).files()
if len(files) != 3:
    # we should have three files match
    PrintError("Expected 3 files, got " + str(len(files)))
    
env.StaticLibrary(files)

