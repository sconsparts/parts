Import('*')

env.PartName("packaging")
env.DependsOn([Component('dpkg_package5')])
#env.DependsOn([Component('DEBIAN')])

out = env.DPKGPackage('test5', sources = ['pkg5','pkg_control'])
env.Alias("${PART_SECTION}::alias::${PART_ALIAS}::dist",env.CCopy("#dist",out))[0]

out=env.ZipPackage(target = 'install.zip', sources = ['pkg5', 'pkg_control'])
env.Alias("${PART_SECTION}::alias::${PART_ALIAS}::dist",env.CCopy("#dist",out))[0]
 
