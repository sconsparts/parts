Import('*')

env.PartName("packaging")
env.DependsOn([Component('archive_package3')])

out=env.TarPackage('install',sources = ['pkg3','pkg_control'])
env.Alias("${PART_SECTION}::alias::${PART_ALIAS}::dist",env.CCopy("#dist",out))[0]

out=env.TarGzPackage('install',sources = ['pkg3','pkg_control'])
env.Alias("${PART_SECTION}::alias::${PART_ALIAS}::dist",env.CCopy("#dist",out))[0]

out=env.ZipPackage('install',sources = ['pkg3','pkg_control'])
env.Alias("${PART_SECTION}::alias::${PART_ALIAS}::dist",env.CCopy("#dist",out))[0]

out = env.DPKGPackage('test3', sources = ['pkg3','pkg_control'])
env.Alias("${PART_SECTION}::alias::${PART_ALIAS}::dist",env.CCopy("#dist",out))[0]