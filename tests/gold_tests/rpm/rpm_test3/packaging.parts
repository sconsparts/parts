Import('*')

env.PartName("packaging")

out=env.RPMPackage('${NAME}-${VERSION}-${RELEASE}.${TARGET_ARCH}',sources = ['pkg1','pkg_control1'],TARGET_ARCH = 'x86_64',NAME = 'Software_Intel', VERSION = '1.0', RELEASE = '1', arch_mapper = {'x86_64':'x86_64'})
env.Alias("${PART_SECTION}::alias::${PART_ALIAS}::dist",env.CCopy("#dist",out))[0]

