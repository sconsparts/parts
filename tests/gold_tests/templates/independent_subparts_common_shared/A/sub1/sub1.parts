Import('*')

env.PartName('sub1')

env.DependsOn([Component("common.subfile")])

#files
c_files=['a_so.c']

outputs=env.StaticLibrary('${PART_NAME}_${PART_SHORT_VERSION}',c_files)
env.InstallTarget(outputs)
